apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: app_name                      # Placeholder
  namespace: argocd                    # Placeholder
  name: deployment               
spec:
  selector:
    matchLabels:
      app: app_name                     # Placeholder
  replicas: 3                           # Placeholder
  template:
    metadata:
      labels:
        app: app_name                   # Placeholder
    spec:
      containers:
        - name: app_name               # Placeholder
          image: image_name             # Placeholder
          imagePullPolicy: Always
          env:
          - name: TZ
            value: "Asia/Kolkata"
                          
